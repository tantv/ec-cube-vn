{% if product.Tags is not empty %}
  <div class="product-tags os-tag {{class|default('mb-2')}}">
    {% for Tag in product.Tags %}
      <span class="badge badge-info os-tag__info">{{ Tag }}</span>
    {% endfor %}
  </div>
{% endif %}
